THIS PROJECT is for learning RBTree.

So it's unstable. DON'T USE in production.

However, Search() and Insert() is stable. Delete() will misbehave in some conditions causing panic.( don't know why. )
